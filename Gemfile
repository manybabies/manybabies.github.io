source 'https://rubygems.org'

gem "github-pages", '197', group: :jekyll_plugins

# enable tzinfo-data for local build (Windows)
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]
gem 'jekyll-paginate'

# add to avoid polling ofor changes (Windows)
gem 'wdm', '>= 0.1.0' 
